

export default function MainPages( { children } ) {
    return (
        <section className="mx-auto max-w-5xl px-4 py-6 sm:px-6 lg:px-8 2xl:mx-auto">
            {children}
        </section>
    );
}